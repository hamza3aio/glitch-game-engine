{
  "name": "markley-3d-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal 3D web game engine: loop, WebGL2 rendering, ECS, physics, input, audio.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
